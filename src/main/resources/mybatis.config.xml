<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="UNPOOLED">
                <property name="driver" value="com.mysql.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/wkh_mock_cms"/>
                <property name="username" value="root"/>
                <property name="password" value="rootpass"/>
            </dataSource>
        </environment>
    </environments>
    <mappers>
        <!--could use 'package name' instead to prevent having to manually list each mapper-->
        <mapper class="com.tw.wkh.mappers.ProdOfMapper" />
    </mappers>
</configuration>